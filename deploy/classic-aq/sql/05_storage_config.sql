-- Storage configuration is written only through PKG_NFE_DEPLOY_CONFIG.
-- The environment template names an existing DBMS_CLOUD credential; no secret
-- material, provider SDK, or provider-specific URI is stored in this package.
prompt PASS: S3-compatible configuration API installed.
