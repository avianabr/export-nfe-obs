-- Copy to ../s3-credential.sql, protect it, and replace all markers.
-- Never commit this file or expose its access key / secret key.
define DEPLOY_S3_CREDENTIAL_NAME = '<credential-name>'
define DEPLOY_S3_ACCESS_KEY = '<s3-access-key>'
define DEPLOY_S3_SECRET_KEY = '<s3-secret-key>'
